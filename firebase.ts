// Firebase system has been removed.
// This file is kept as a placeholder to prevent import errors during migration if referenced elsewhere,
// though App.tsx has been updated to stop using it.

export const auth = null;
export const db = null;
export const isConfigured = true;
export const saveConfigToStorage = () => {};
